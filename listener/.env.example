RPC_URL=https://eth-sepolia.g.alchemy.com/v2/SUA_KEY
CONTRACT_ADDRESS=0x...
SUPABASE_URL=https://xxxx.supabase.co

# Chave service/secret do Supabase (Settings → API → service_role)
SUPABASE_SECRET_KEY=sb_secret_...

# Bloco em que o contrato foi deployado — acelera o replay
# Encontre em: etherscan.io → busca pelo CONTRACT_ADDRESS → "Contract Creation" block
DEPLOY_BLOCK=8000000
